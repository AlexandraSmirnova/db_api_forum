__author__ = 'AS'
from common.DB_connect import *

def save_forum(connect, name, short_name, user):
    print('save_forum:')
    DB_connect.update_guary(connect, "INSERT INTO forum (name, short_name, user) VALUES (%s, %s, %s)', (name, short_name, user)")
    fotum = DB_connect.select_query(connect, 'select id, name, short_name, user FROM forum WHERE short_name = %s', (short_name, ))
    return forum_description(forum)

def forum_description(forum):
    forum = forum[0]
    response = {
        'id': forum[0],
        'name': forum[1],
        'short_name': forum[2],
        'user': forum[3]
    }
    print(response)
    return response

    
